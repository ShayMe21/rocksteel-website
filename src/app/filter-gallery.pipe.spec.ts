import { FilterGalleryPipe } from './filter-gallery.pipe';

describe('FilterGalleryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGalleryPipe();
    expect(pipe).toBeTruthy();
  });
});
